import { Observable } from 'rxjs';
import { Title, User } from '../model/misc.model';
export declare abstract class UserAccountFacadeTransitionalToken {
    abstract get(): Observable<User | undefined>;
}
export declare abstract class UserProfileFacadeTransitionalToken {
    abstract getTitles(): Observable<Title[]>;
}
